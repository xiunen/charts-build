const config = {
  port: 8080,
  distPath: 'dist',
  sourcePath: 'src',
  viewPath: './server/views',
  assetsMap: 'dist/assets.json',
  storeName: 'preloadedState',
  ssr: true
}

module.exports = config;
